{
  "name": "suimini-realtime-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Relais WebSocket pour le temps réel Railway (LISTEN/NOTIFY 'tree_changes' → clients). Service AUTONOME, hébergé à côté de l'app Next.js (Vercel ne tient pas de connexion persistante). Voir docs/railway-realtime-plan.md.",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.22.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "@types/pg": "^8.20.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
